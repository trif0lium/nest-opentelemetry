import { characterApiDataGateway } from './character-api-data-gateway';

describe('characterApiDataGateway', () => {
  it('should work', () => {
    expect(characterApiDataGateway()).toEqual('character-api-data-gateway');
  });
});
